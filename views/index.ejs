<h2 class="page-header">Recently Added Books</h2>
<div>
  <% books.forEach(book => { %>
    <a href ="/books/<%= book.id%>">
      <img class="front-page-book-covers" src="<%= book.coverImagePath %>">
    </a>
  <% }) %>
</div>